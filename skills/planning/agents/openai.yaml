interface:
  display_name: "Planning"
  short_description: "Write repo plans and set up native goals."
  default_prompt: "Use $maximilian-universal-workflow:planning with repo evidence to write a decision-complete plan, resolve native goal state, create the default goal only when the Goal Tool Gates allow it, update the phase footer, and hand off through git-worktrees before execution."

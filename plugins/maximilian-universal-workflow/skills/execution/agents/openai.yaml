interface:
  display_name: "Execution"
  short_description: "Execute approved repo plans inline or with isolated workers."
  default_prompt: "Execute this approved repository plan within allowed side effects, preserve acceptance criteria, integrate worker outputs, and continue to verification."

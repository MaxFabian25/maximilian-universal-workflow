interface:
  display_name: "Review"
  short_description: "Run read-only reviews before handoff."
  default_prompt: "Review this repository workflow output read-only against acceptance criteria, report evidence-backed findings, update the phase footer, and continue to handoff or execution."

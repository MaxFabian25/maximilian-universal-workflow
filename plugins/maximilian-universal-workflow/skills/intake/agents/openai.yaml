interface:
  display_name: "Intake"
  short_description: "Start the repo workflow loop and advance to the right phase."
  default_prompt: "Run the repo workflow loop for this request: confirm state, choose the current phase, and continue until a decision or stop condition is reached."

interface:
  display_name: "Ideation"
  short_description: "Brainstorm repo-backed alternatives and converge on a direction."
  default_prompt: "Generate repo-backed implementation-path options, use request_user_input for the direction choice, capture acceptance criteria and allowed side effects, update the phase footer, and prepare the planning transition."

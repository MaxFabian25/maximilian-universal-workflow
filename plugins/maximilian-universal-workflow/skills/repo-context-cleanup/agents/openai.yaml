interface:
  display_name: "Repo Context Cleanup"
  short_description: "Inventory and clean stale agent planning residue."
  default_prompt: "Inventory stale repo context, classify it, use request_user_input for cleanup policy choices, and return an updated phase bundle."

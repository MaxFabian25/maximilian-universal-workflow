interface:
  display_name: "Multi-Agent V2"
  short_description: "Coordinate Codex MultiAgentV2 task paths."
  default_prompt: "Use the bundled Multi-Agent V2 workflow to delegate bounded subagent tasks, collect results through list_agents, recover stuck agents once, and close finished children."

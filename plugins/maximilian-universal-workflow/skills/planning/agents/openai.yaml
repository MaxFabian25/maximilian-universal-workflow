interface:
  display_name: "Planning"
  short_description: "Write goal-backed repo execution plans."
  default_prompt: "Use repo evidence, write the plan, and prepare the /goal execution launch prompt."

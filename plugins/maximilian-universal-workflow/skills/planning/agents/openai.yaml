interface:
  display_name: "Planning"
  short_description: "Write repo plans and set up native goals."
  default_prompt: "Use repo evidence to write a decision-complete plan, resolve native goal state, create the default goal, update the phase footer, and hand off through git-worktrees before execution."

interface:
  display_name: "Exploration"
  short_description: "Gather repo evidence with read-only exploration and explorer fanout."
  default_prompt: "Explore this repository task, gather cited evidence, identify acceptance criteria implications, emit the phase-transition packet, and continue to ideation or planning."
